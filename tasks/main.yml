---
# tasks file for apache
- name: install apache
  apt:
    name: apache2
    state: latest
  become: yes

- name: restart apache
  service:
     name: apache2
     state: started
     enabled: yes
  become: yes
